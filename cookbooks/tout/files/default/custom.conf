# Drop requests to non-rails requests
if ($request_filename ~* \.(aspx|php|jsp|cgi)$) {
  return 410;
}

# Ensure SSL unless POST
if ($request_method != POST) {
  rewrite ^ https://$host$request_uri permanent;
}