if ($host ~* connect\.toutapp\.com) {
  rewrite ^ https://toutapp.com/c$request_uri permanent;
}

if ($request_method != POST) {
    set $test P;
}

if ($host ~* www\.toutapp\.com) {
  set $test "${test}C"
}

if ($test = PC) {
  rewrite ^ https://toutapp.com$request_uri permanent;
}