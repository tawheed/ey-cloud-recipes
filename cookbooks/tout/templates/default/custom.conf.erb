# Drop requests to non-rails requests
if ($request_filename ~* \.(aspx|php|jsp|cgi)$) {
  return 410;
}

set $subdomain "www"
if ($host ~* connect\.(.*)) {
  set $subdomain "connect"
}

rewrite ^ https://$subdomain.toutapp.com$request_uri permanent;